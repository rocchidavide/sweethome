smart_gaps on
# smart_borders on
gaps inner 30
gaps outer -4
bindsym $mod+Shift+d    gaps inner all set 0; gaps outer all set 0; border normal
bindsym $mod+Shift+t    gaps inner all set 30; gaps outer all set -4; border pixel 1